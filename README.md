This project will need some setting up, as it turns out, I can't import any file into this repository if it's greater than 25MB

When I submitted this project, I also sent the Archive with the necessary CSV files. Just make sure they are from 4.12.16-5.12.16. 

The CSV files you should bring into the directory you will run the Jupyter notebook are:

dailyActivity_merged.csv,
heartrate_seconds_merged.csv,
hourlyCalories_merged.csv,
hourlyIntensities_merged.csv,
hourlySteps_merged.csv,
minuteCaloriesNarrow_merged.csv,
minuteIntensitiesNarrow_merged.csv,
minuteMETsNarrow_merged.csv,
sleepDay_merged.csv,
weightLogInfo_merged.csv

Once you've imported them, make sure you run the Import Statements so that the code will work.

I should mention that for the minute data, it took me 3 minutes 20 seconds to run one cell (I commented it in the notebook, but just a heads up).
